## Agent skills

### Issue tracker

Specs and implementation issues are tracked as local Markdown under `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Domain docs

This is a single-context repository: domain vocabulary lives in root `CONTEXT.md`, and architectural decisions live in `docs/adr/`. See `docs/agents/domain.md`.

