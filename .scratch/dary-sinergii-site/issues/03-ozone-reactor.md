# 03 — Вариант «Реактор O₃»

**What to build:** A complete dark technical variant across all routes, using a bounded cyan particle stream, diagnostic scanning, terse labels, and compact sectional rhythm to make the ozone process the central spectacle.

**Blocked by:** 02 — Переключатель четырёх вариантов

**Status:** done

- [x] Home and every inner route have a coherent Reactor composition, navigation, controls, and responsive behavior.
- [x] The hero exposes the shared video slot and uses a clearly synthetic Canvas 2D fallback.
- [x] Diagnostic overlays communicate real categories or explicit placeholders, never pseudo-scientific data.
- [x] Mobile and reduced-motion modes remain complete without continuous particles or scanning.

## Implementation notes

- Variant assets are isolated under `src/variants/`; all four CSS/JS boundaries are prewired so tickets 04–06 can work without changing shared build or renderer asset loading.
- The Reactor runtime uses one capped Canvas 2D coordinator, deterministic reduced-motion frames, mobile particle budgets, visibility pausing, and IntersectionObserver activation.
- The source-free `<video>` boundary has no fake controls; visible copy identifies the current stage as a procedural placeholder.
- `npm test`, JavaScript syntax checks, headless Edge desktop/mobile smoke captures, and the Impeccable detector pass completed successfully.
