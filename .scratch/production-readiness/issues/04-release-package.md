# Архив подготовленного выпуска

Status: done

Подготовить архив локальных исправлений для загрузки через существующий парольный
SSH без публикации в GitHub. Включить только исходники, ресурсы и выбранную
документацию; исключить секреты, клиентские данные, локальные копии и инструменты
агента. Приложить контрольные суммы и проверить состав архива.

## Исходные данные

Разработчик подтвердил `/srv/dari/current -> /srv/dari/releases/email-76d6352`
и Node.js `v22.23.2`. Новый SSH-вход после включения UFW успешен.
На момент сборки результат запуска свежей резервной копии ещё не был получен.
Позднее разработчик подтвердил Result=success, ExecMainStatus=0 для dari-backup.service.
Выпуск на VPS ещё не выполнялся; настройки, данные и переключение приложения
в этот шаг не входят.

## Результат

Создан `scripts/package-release.py`; Ruff check/format прошли. Подготовлен архив
`artifacts/releases/dari-readiness-20260918.tar.gz`, 331 файл, 37.11 MiB, и внешний
файл `.sha256`. SHA256 архива:
`bbd55bba8a933dc040e07050a66b255590ab2911636c6ad65b5c7c0574c190ce`.
Каждый файл прочитан обратно из архива и побайтово проверен; внутри находятся
manifest с базовым commit и хешами, а также SHA256SUMS. Секретные каталоги,
пользовательские БД и локальные инструменты агента исключены из состава.
При перечислении untracked Git сообщил о недоступных подкаталогах `.scratch/pdf-tools`;
этот каталог не входит в набор упаковываемых путей. Выгрузка и выпуск не выполнялись.
