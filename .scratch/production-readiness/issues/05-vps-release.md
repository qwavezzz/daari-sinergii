# Выпуск исправлений на VPS

Status: ready-for-human

Разработчик подтвердил загрузку архива и SHA256; просит следующий шаг установки.
Подготовлены команды извлечения в новый каталог, проверки содержимого, сравнения
основных Nginx/systemd-файлов с действующими и штатного выпуска. При отличиях
команды завершаются до переключения. Подготовлен откат к `email-76d6352`.

Инструкция: `deploy/INSTALL-READINESS-20260918.md`. Агент не подключён к VPS;
разработчик выполнил выпуск и подтвердил переключение ссылки и активность служб.
Код приложения в загруженном архиве совпадает с проверенным локальным. После
сборки архива менялись документы, список упаковки и служебные HTTP-проверки.
Загруженный архив не пересобирался.

## Результат и оставшаяся приёмка

Установка Pillow 12.3.0, сборка, роли и collectstatic прошли; миграций нет. Первый
curl после restart/reload вернул код 7, уже после переключения. Последующие локальная
и внешние health-проверки — 200. Основной аудит: 68 HTTP-ответов без неожиданных
ошибок, отдельно 3 успешных запроса admin CSS/poster. SEO и CSP исправления видны
в production. Повторный запрос к публичному домену с VPS снова вернул curl 7 / HTTP 000,
в том числе с --noproxy '*'. Getent на VPS вернул старый IP 31.31.197.73;
прямой запрос через --resolve на 134.0.113.203 успешен. Локальной записи домена
в hosts нет; resolvectl подтвердил Data from: cache для старого адреса.
После шага очистки resolvectl снова дал старый IP, теперь Data from: network.
На ens3 настроены 194.58.125.120 (текущий) и 194.67.94.40, домен msk3.cloud.reg.ru.
Google/Cloudflare независимо возвращают новый IP для основного домена и shop.
После предложенного временного переключения ens3 разработчик подтвердил обычный
HTTPS без --resolve: status=ok, HTTP 200, IP 134.0.113.203. Доступ по домену с VPS
восстановлен. Источник конфигурации затем установлен: Netplan/cloud-init/networkd.
Постоянный DNS drop-in установлен; после переходного состояния DHCP прежние адрес
и маршрут восстановились. Разработчик подтвердил A=134.0.113.203 для обоих доменов
с `Data from: network`, оба `/health/` с VPS без `--resolve` — status=ok и HTTP 200.
Подробности — issue 06. После перезагрузки DNS ещё не проверялся.
VPN не отключать. Ручной вход/редактирование в админке ещё не проверен.

Локальный release.sh теперь повторяет connection-refused с ограничением времени
и при окончательной ошибке явно сообщает об уже переключённом коде. Синтаксис Bash
и три реальные проверки на локальном HTTP-сервере прошли (refused -> 200, 502 -> 200,
404 -> явный отказ). Первый тест не воспроизвёл закрытый порт; исправленная
симуляция прошла. Изменение скрипта на VPS пока не переносилось.
Скрипт публичного аудита получил --output, чтобы сохранить исходный отчёт отдельно.
