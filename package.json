{
  "name": "dary-sinergii-site",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free static site generator for «Дары Синергии».",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/dev.js",
    "verify": "node scripts/verify.js",
    "test": "node scripts/build.js && node scripts/verify.js"
  }
}
